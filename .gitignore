node_modules/
package-lock.json
.DS_Store
*.log
.env
.env.*

.idea/

.claude/settings.local.json
.claude/worktrees/
.claude/

.playwright-mcp/

# Local-only brainstorming specs & implementation plans — stay on disk, never committed.
docs/superpowers/

# Written by CI to authenticate against the private JFrog feed — never commit it.
.npmrc
