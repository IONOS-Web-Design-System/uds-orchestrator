<!-- THE SHOT-AUTHORITY CLAUSE — spliced in whenever the brief pins ANY shot.

     It follows the shot clause and settles the conflict between the pinned framing and a
     Feature that asks for a whole-body view. Its own file because its condition is different
     from any single shot's: it is emitted for all three, so restating it in each would be three
     copies to keep aligned.

     The whole-body phrases are quoted because the model has to recognise them in the Feature to
     ignore them — this is the model's own vocabulary doing its job, not an example handed over
     for copying.

     DELIVERED VERBATIM by loadCraftContext (image-svc/src/craft/skills.ts): the LINE BREAKS
     ARE SEMANTIC — what reads as a paragraph below is ONE long line, as the TypeScript template
     literal this replaced produced, and re-wrapping it at 80 chars changes the prompt bytes.
     This comment is stripped before delivery on every profile, so it is author-only and must
     never carry instruction. -->

The shot framing above is AUTHORITATIVE: if the Feature text implies ANY different framing/crop or a whole-body view — e.g. "full-body", "head to toe", "full-length", "fully visible from head to toe", "entire body", "whole figure", "fully visible", "the figure stands"/"standing", "clean/complete silhouette" — IGNORE it and follow the shot framing above (crop at the point it specifies, e.g. the waist), NOT the Feature.
