<!-- THE REFRAME REMEDY — spliced into craft-plausibility.md at {{reframeClause}}, and ONLY when
     nothing has fixed the shot yet: no injected `Photographic camera:` preset and no `condition`
     reference photograph.

     Its own file because its condition is narrower than the block's, exactly as
     craft-treatment-room.md's is narrower than craft-treatment.md's.

     WHY CONDITIONAL AT ALL. Measured (15 reps, gemini-3.1-flash-lite, craftProfile=minimal, the
     b2-homepl case): offered unconditionally this clause produced two of fifteen outputs carrying
     two contradictory camera statements at once — the injected preset's framing AND an
     over-the-shoulder reposition — because on `minimal` the camera preset below already fixes the
     shot. Suppressing the clause when the shot is fixed leaves the blank-screen remedy, which is
     the only one that can be true under a camera the model does not control.

     The wording is the craft corpus's OWN ("move or widen the camera — do not twist the device
     toward the lens", shared-image-type-device-focused.md). Naming a decision abstractly is what
     fails here; naming the model's own phrasings is what moved a metric 46 points.

     LEADING SPACE IS SUPPLIED BY THE CALL SITE, not here — a trailing or leading space in a
     markdown file does not survive an editor that tidies whitespace, and loadCraftContext trims.

     DELIVERED VERBATIM: this comment is stripped before delivery on every profile — author-only,
     never instruction. -->

Put the camera past their shoulder if you want the interface described: the screen's own face is then toward the lens. Move the camera, never the device.
