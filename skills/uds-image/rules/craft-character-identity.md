<!-- THE NO-PERSONA FALLBACK — emitted when the brief wants a person but uds-showroom has no
     persona file for this (brand, showroom). Mutually exclusive with craft-persona-framing.md.

     {{market}} is the brief's market, defaulted by the CALL SITE (to 'de') rather than here: a
     default written into the prose would be invisible to the code that reads the field.

     It deliberately does NOT describe a face, a build or a wardrobe. The brand
     character-ethnicity rule owns identity, and this file only says where to get it and that the
     Feature keeps every scene field — the same split craft-persona-framing.md states.

     DELIVERED VERBATIM (one long line after the heading); comment stripped before delivery. -->

# Character identity — no fixed persona

No persona is defined for this showroom. Invent a NEUTRAL, market-appropriate professional consistent with the brand character-ethnicity rule in the skills above (market: {{market}}). Keep the identity understated and realistic; the Feature above still owns every SCENE field — action, pose, expression, setting, props, framing context.
