<!-- THE 'waist-up-hero' SHOT-FRAMING CLAUSE — spliced into craft-task-cutout.md when the brief pins shot='waist-up-hero'.

     One file per shot value, chosen by an explicit map in image-svc (never by interpolating the
     brief into a filename). It CONTINUES the sentence above it, so it carries no heading and no
     trailing punctuation-plus-space: the joining space comes from the call site.

     DELIVERED VERBATIM by loadCraftContext (image-svc/src/craft/skills.ts): the LINE BREAKS
     ARE SEMANTIC — what reads as a paragraph below is ONE long line, as the TypeScript template
     literal this replaced produced, and re-wrapping it at 80 chars changes the prompt bytes.
     This comment is stripped before delivery on every profile, so it is author-only and must
     never carry instruction. -->

Frame the subject WAIST-UP or CHEST-UP — a three-quarter hero portrait or a dynamic candid; the BOTTOM edge crops at roughly the waist/hips — NOT a full-length, head-to-toe, or standing full-body pose.
