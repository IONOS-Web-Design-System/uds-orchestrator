<!-- THE ROOM-SPECIFICITY CLAUSE — appended to craft-treatment.md when, and only when, an
     Environment line was injected.

     Its own file because its condition is narrower than the block's: the camera and lighting
     axes can resolve while the environment axis misses (no catalog for `outdoors`, `cafe` or
     `other`), and with no room stated this paragraph would point at nothing.

     The material nouns are deliberate and concrete. They are the vocabulary the craft model
     itself uses, which is what made the difference between an ignored instruction and a followed
     one; an abstract "preserve the environment" moved nothing.

     DELIVERED VERBATIM by loadCraftContext (image-svc/src/craft/skills.ts): the LINE BREAKS
     ARE SEMANTIC, and this comment is stripped before delivery on every profile — author-only,
     never instruction. -->

For the room, name its SPECIFIC materials and features as part of your description: the stone, brick, timber, concrete, resin, glazing, ceilings, floors, counters and fittings it actually names. Substituting a generic setting for the one named here is the single most common way this is lost.
