<!-- THE SCREEN-CONTENT REFERENCE BLOCK — emitted only when a reference image is supplied AND
     referenceRole === 'screen-content'. The sibling craft-reference-condition.md covers the other
     role; the two are mutually exclusive and image-svc decides which, from one boolean computed
     once (referenceIsCondition in craft/prompt.ts) so the block and the axis suppression can
     never disagree about which role is in play.

     A 'screen-content' reference suppresses NOTHING: it is pixels destined for a device screen
     and it makes no claim about the photograph, so the camera, lighting and environment lines all
     stand. That is why this file argues only about the screen.

     DELIVERED VERBATIM: the LINE BREAKS ARE SEMANTIC (the paragraph is one long line, as the
     template literal this replaced produced), and this comment is stripped before delivery on
     every profile — author-only, never instruction. -->

# Reference image (screen content) — AUTHORITATIVE

A REFERENCE IMAGE is supplied: it is the EXACT user interface to display ON the device's screen. Render it CRISPLY and LEGIBLY as the on-screen content — fitted to the screen bezel with correct perspective, sharp and readable, the clear focal content of the screen. This OVERRIDES any other guidance about keeping the screen "blank", "generic", "minimal", or "suggestive", and any "screen content is not the point" rule: for THIS image the on-screen UI IS shown clearly. Do NOT blank or genericize the screen, and do NOT reproduce the reference as a floating element or background — it goes ON the screen. (The device stays the hero and any person stays partial context.)
